# 发布验收清单

验收日期：2026-06-06
负责人：AI-5
范围：四个 AI 的内容、路线、交互、素材成果总集成，以及公开发布前安全与合规检查。

## 一、结论

- 当前源码通过 `npm run lint`、`npx tsc --noEmit`、`npm run build`。
- 签文字段审计、路线数量审计、权重审计、素材引用审计通过。
- 页面首屏、metadata、manifest、OpenGraph 图和 README 已明确标注“非官方东方 Project 二次创作”。
- 运行配置未引用 `legacy/`，未引用原作游戏截图、拆包素材或官方图。
- 公开版本未发现收费解锁或付费门槛。
- 早期 Touhou LostWord 官方角色图已从 `public/images/characters/lostword/` 移除，避免公开构建带出官方图。
- 2026-06-06 追加发布收口：默认公开构建不加载本地角色图和本地 MP3；`build:public` 会裁剪 GitHub Pages 静态包中的 `images/characters`、`audio/music`、`generated-themes/legacy`、raw PNG 场景 / 仪式源图与 source sheet。

## 二、按 AI 分区复核

| AI | 负责方向 | 本轮复核结果 |
| --- | --- | --- |
| AI-1 | 签文与东方口吻扩充 | `fortunes.json` 共 184 条；无重复 id；类型必填字段缺失 0；14 条正式路线均不少于 12 条签文；权重总和为有限数 |
| AI-2 | 场景文案与路线语气系统 | 14 条路线均有独立仪式命名、纸张、道具、声音方向、阶段文案和结果标签；页面元信息已使用 14 路线与非官方二创口径 |
| AI-3 | UI、交互、无障碍与抽签稳定性 | 浏览器回归覆盖首屏、3 条路线抽签、设置弹窗、音频开关、收藏、签册筛选、375px 窄屏；已修复设置音频开关时清空当前路线的集成问题 |
| AI-4 | 素材、性能与公开发布包 | 图片和音频运行引用缺失 0；`legacy/` 未回接；发布包体积与授权风险已记录；官方图滞留公开目录问题已由 AI-5 移除 |

说明：未发现独立命名的 AI-1 到 AI-4 完成报告文件；本轮依据 `docs/后续计划.md`、`docs/素材资产审计.md`、`docs/五AI并行优化分工手册.md` 和当前 git diff 复核各分区成果。

## 三、自动审计

| 审计项 | 结果 |
| --- | --- |
| 签文总数 | 184 |
| 重复 id | 0 |
| 类型必填字段缺失 | 0 |
| 可选推荐字段缺口 | 49 个 `archetype` 缺口，组件已兜底为“幻想乡来客” |
| 路线数量 | 14 |
| 各路线签文数 | 19、14、16、19、12、12、14、12、14、12、18、12、12、12 |
| 权重审计 | 总权重 `5709.53`，异常权重 0 |
| 图片引用 | 运行面与签文角色图片引用 160 个，缺失 0；AI-4 的素材包细分审计见 `docs/素材资产审计.md` |
| 音频引用 | 运行配置与 `tracks.json` 引用 5 个 MP3，缺失 0 |
| `legacy/` 回接 | 0 |
| 收费 / 付费功能关键词 | 0 |
| 官方图运行引用 | 0 |

## 四、验证命令

```powershell
npm audit --audit-level=moderate
npm run lint
npm run test
npx tsc --noEmit
npm run build
npm run build:public
```

结果：

- `npm run lint`：通过。
- `npm run test`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过，Next.js 16.2.6 静态页面构建成功。
- `npm run build:public`：通过，GitHub Pages 静态导出后执行公开包裁剪。
- `npm audit --audit-level=moderate`：仍报告 Next 内置 `postcss@8.4.31` moderate advisory，自动修复会强制降级到 `next@9.3.3`，未执行 `npm audit fix --force`。

本轮修复后 `npm run lint`、`npx tsc --noEmit`、`npm run build` 已连续三轮通过。

## 五、浏览器回归

本机已有开发服务运行在：

- `http://localhost:4123`

回归工具：headless Chrome + Chrome DevTools Protocol。

覆盖路径：

- 桌面首屏：显示 14 条路线，并显示“非官方东方 Project 二次创作”声明。
- 路线抽签：博丽神社、红魔馆、永远亭均可进入并完成结果签面。
- 音频开关：设置弹窗内“背景环境音”和“仪式音效”可关闭并重新开启，且不会清空当前路线。
- 收藏：博丽神社抽签结果可收入本地签册。
- 签册筛选：进入 `/collection` 后可按“博丽神社”筛选。
- 设置弹窗：可打开、操作并关闭。
- 移动端窄屏：375px 宽度下无横向溢出，14 路线文本可见，首屏二创声明可见。

## 六、合规检查

- 已确认官方英文指南要求：清楚标注 fan work、不得误认为官方、不得包含官方游戏抽取素材，浏览器游戏只能免费发布。来源：`https://touhou-project.news/guidelines_en/`。
- README 与页面元信息已标注“非官方东方 Project 二次创作”。
- 本项目不声明官方授权，也不使用会让用户误认为官方项目的描述。
- 当前公开版本未设置付费解锁。
- `unlockedVisualTheme` 仅为稀有签视觉主题字段，不是付费解锁字段。
- `public/images/characters/lostword/` 中早期官方角色图已移除。

## 七、残余风险

| 优先级 | 风险 | 处理路径 |
| --- | --- | --- |
| 中 | `public/audio/music/*.mp3` 来源字段乱码且授权证据不足 | 默认公开运行已关闭本地 MP3，`build:public` 会从 `out/` 移除 `audio/music`；若未来公开启用 BGM，必须替换为自有 / 可授权 / 可署名音频 |
| 中 | GitHub Pages 静态包可能带出 raw PNG、source sheet、legacy 和授权未确认素材 | `build:public` 已裁剪这些目录 / 文件；后续 GitHub Pages 工作流应调用 `npm run build:public` 而不是普通 build |
| 中 | Next 内置 PostCSS advisory | 等待 Next 安全补丁或兼容升级；不要使用 `npm audit fix --force` 降级 Next |
| 中 | 萌娘百科角色图公开传播授权与署名 | 默认公开运行已关闭本地角色图，`build:public` 会从 `out/` 移除 `images/characters`；如未来公开启用角色图，需逐项确认授权、署名和来源链接 |
| 中 | 严格 CSP 尚未启用 | 公开域名和资源清单稳定后再配置 CSP，并回归图片、字体、音频和 Next 运行脚本 |

## 八、AI-5 跨区修复说明

| 文件 | 原因 | 影响 | 验证 |
| --- | --- | --- | --- |
| `eslint.config.mjs` | ESLint 覆写默认忽略后扫入 `tmp/chrome-ai3-cdp` 外部扩展脚本，导致全量 lint 门禁失败 | 只忽略本地临时目录 `tmp/**` 和 `.playwright-mcp/**`，不影响源码 lint | `npm run lint` 通过 |
| `src/constants/design-tokens.ts` | 旧的 BGM fallback 列表保留 6 个不存在的主题曲文件名，且文案容易被误读为官方曲目 | 改为 5 条与 `tracks.json` 和公开目录一致的本地音轨占位项；不改变优先读取 manifest 的运行逻辑 | 素材引用审计确认音频缺失 0；`npx tsc --noEmit` 通过 |
| `src/components/daily-draw-panel.tsx` | 首屏缺少可见“非官方东方 Project 二次创作”声明，合规证据不足 | 增加小型声明标签，不改变路线选择结构 | 浏览器首屏和 375px 窄屏均确认声明可见 |
| `src/store/omamori-store.ts` | 设置音频 / 动效时会因 `askRouteOnLaunch` 清空当前路线，导致用户回到路线选择 | 仅当修改默认入口或重新选择入口设置时才调整 `selectedRouteId` | 浏览器回归确认两个音频开关可操作且不清空当前路线 |
| `public/images/characters/lostword/*` | 9 张早期 Touhou LostWord 官方网站角色图虽未被引用，但在 `public/` 下会被公开构建带出 | 删除未引用官方图，避免公开包包含官方图 | 引用搜索只剩 README 说明；自动审计运行引用为 0 |
| `src/lib/public-assets.ts`、`src/components/fortune-card.tsx`、`src/components/daily-draw-panel.tsx`、`src/lib/audio.ts` | 本地角色图和 BGM 授权仍需公开前逐项确认 | 默认公开构建不加载这些本地素材；私用时可用 `NEXT_PUBLIC_ENABLE_LOCAL_CHARACTER_IMAGES=true` 和 `NEXT_PUBLIC_ENABLE_LOCAL_MUSIC=true` 显式开启 | `npm run test` 覆盖默认关闭；浏览器回归确认结果签面使用风格化占位 |
| `scripts/prune-public-export.mjs`、`package.json` | GitHub Pages 静态导出会复制整个 `public/` | 新增 `build:public`，构建后裁剪角色图、本地音乐、legacy、raw PNG / source sheet | `npm run build:public` 通过，裁剪报告显示 `out/` 包体积下降 |

## 九、下一轮建议

1. AI-1：后续新增签文继续跑字段、重复 id、路线覆盖和模板化审计。
2. AI-2：继续统一公开文案，避免任何“官方授权”“候选 / 待开放”误导口径。
3. AI-3：补键盘完整路径、结果弹窗焦点恢复、降低动效模式和更多移动端机型回归。
4. AI-4：若未来要公开启用角色图或 BGM，优先替换为项目自制 / 明确授权素材，不再批量新增概念图。
5. AI-5：发布前再次执行本清单，确认 GitHub Pages 工作流使用 `npm run build:public`，并补真实域名、CSP、GitHub Pages 响应头复核。
